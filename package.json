{
  "devDependencies": {
    "@types/node": "20.4.6",
    "gts": "^5.0.1",
    "three": "^0.155.0",
    "typescript": "~5.1.6"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
